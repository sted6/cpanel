import { SocialSecurityPipe } from './social-security.pipe';

describe('SocialSecurityPipe', () => {
  it('create an instance', () => {
    const pipe = new SocialSecurityPipe();
    expect(pipe).toBeTruthy();
  });
});
